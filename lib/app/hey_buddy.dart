import 'package:flutter/material.dart';

class HeyBuddy extends StatelessWidget {
  const HeyBuddy({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Hey Buddy'),),
    );
  }
}